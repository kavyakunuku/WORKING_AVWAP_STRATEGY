"""Single build version for the whole system.

Shown in the dashboard footer and /api/state payload so support questions
("which build are you running?") are answered at a glance. Bump on every
shipped build.
"""
APP_VERSION = "2026-09-17b"
