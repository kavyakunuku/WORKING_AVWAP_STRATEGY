# Core runtime dependencies
requests>=2.31
Flask>=3.0
psutil>=5.9
# Windows only: Python's zoneinfo needs the IANA tz database (Linux/macOS ship
# it with the OS). The app falls back to fixed UTC+05:30 without it, but with
# it you avoid the warning entirely.
tzdata>=2024.1; platform_system == "Windows"

# Optional (live order-update websocket, only if you want push-based order updates)
# dhanhq>=2.0
# websocket-client>=1.6

# Development / testing
pytest>=8.0
